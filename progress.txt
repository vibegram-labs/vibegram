2026-04-29

Completed:
- Fixed the iOS live-message reopen flicker in native chat rows. `ChatEngine.getChatRows` now returns a stable timestamp-ordered merge of persisted history rows plus live socket rows, so a message that arrived while the chat was open is present in the first row payload when the chat is closed and reopened. The merged row set is persisted back to the per-chat cache after live insert/edit/delete events once history has loaded, and history refreshes now store the merged set instead of dropping live rows that arrived during the refresh. Revalidated with targeted `xcrun swiftc -parse ios/ChatModule/ChatEngine.swift ios/ChatModule/ChatListView.swift ios/Sources/App/AppRootView.swift`, `git diff --check`, `python3 -m json.tool tests.json`, and full simulator `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-ios-live-row-reopen-fix CODE_SIGNING_ALLOWED=NO build`; the build succeeded with existing warnings only.
- Fixed the iOS `Thread 13/21: EXC_BAD_ACCESS` crash in `ChatConversationController.closeOpenedChatChannel()`. The close path can run from controller deinit/background cleanup, so it no longer reads `view.window` off the main thread while scheduling `ChatEngine.shared.closeChatChannel(...)`, and the background close completion no longer creates a `[weak self]` main-queue callback against a controller that may already be partially deallocated. The final close result is logged without touching the controller instance. Revalidated with targeted `xcrun swiftc -parse ios/Sources/App/AppRootView.swift`, `git diff --check -- ios/Sources/App/AppRootView.swift progress.txt tests.json`, `python3 -m json.tool tests.json`, and full simulator `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-ios-close-channel-capture-fix CODE_SIGNING_ALLOWED=NO build`; the build succeeded with existing warnings only.
- Added native incoming/outgoing call surfaces for the standalone Android and iOS shells. iOS now opens a fullscreen native call overlay from profile/header call buttons, maps outgoing starts to `ringing`, observes incoming call engine state, shows accept/end/retry controls, and turns failed or unanswered calls into a retryable call screen instead of only a toast. Android now has an app-owned `NativeCallActivity` for outgoing and incoming user-channel call events, with the same ringing/connecting/failed/retry/end states. Both engines preserve peer metadata in state and expose a local failed state for no-answer timeouts. Revalidated with baseline and final iOS simulator builds, final Android `./gradlew :app:compileDebugKotlin --console=plain`, `git diff --check`, and `python3 -m json.tool tests.json`; builds succeeded with existing warnings only.

Remaining:
- Live-verify on the iPhone path that previously crashed: open a chat, back out/dismiss during channel cleanup, and confirm the Xcode console shows `closeChatChannel finished ...` without `controllerAlive` / `windowAttached` callback logging and without `EXC_BAD_ACCESS`.
- Live-verify the new call UI on devices: from a direct chat profile/header, start voice and video calls, confirm the caller sees `Ringing...`, confirm incoming online recipients see accept/decline, and confirm unanswered or rejected signaling shows the retryable failed state.

2026-04-28

Completed:
- Fixed the Android chat-open crash reproduced on device after tapping the visible `mohammad` chat row. The crash was `Unable to start activity ... ConversationActivity` caused by a `NullPointerException` in `NativeCallEngine.configure(CallEngine.kt:32)`: nullable call config fields were being written into a `ConcurrentHashMap`, which rejects null values. `NativeCallEngine` state now uses the synchronized linked map shape its nullable diagnostic/status fields already require. Revalidated with `./gradlew :app:compileDebugKotlin`, `./gradlew :app:installDebug`, `adb logcat -c`, normal app launch, tapping the same chat row, process/focused-window checks showing `ConversationActivity`, and an empty crash buffer / no recent `AndroidRuntime` fatal logs after opening chat.
- Fixed the native chat/profile layout crash/freeze risk from the posted device logs. The logs did not contain an app exception; they showed OS teardown/network noise while the chat route had recently gained a full hidden profile surface. Android and iOS now mount only the chat surface at chat-open time, create the profile surface lazily when the avatar/profile path is used, update profile rows only while that page is visible, and release the profile view after it slides away. This removes duplicate hidden profile table/avatar/media parsing work from normal chat refreshes, especially large chats. Revalidated with baseline and final Android `./gradlew :app:compileDebugKotlin`, baseline and final iOS device-sdk `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphoneos -configuration Debug -derivedDataPath /tmp/vibe-crash-... CODE_SIGNING_ALLOWED=NO build`, `git diff --check`, and `tests.json` parsing; both builds succeeded with existing warnings only.
- Fixed the follow-up iOS chat reopen/cache regression. Fully loaded native chat rows are now persisted per user/chat and restored into `ChatEngine` before a chat route paints, so a previously loaded conversation no longer opens to an empty wallpaper or a gap-filled preview slice after app reopen. Home row taps now prefer restored native rows instead of `previewRows`, avoiding the first/last-only partial ordering that made middle messages pop in and shift the list. iOS media cache file names now use a stable FNV-style hash instead of Swift `String.hashValue`, and downloaded document previews now persist under Caches instead of Temporary, so downloaded voice/image/video/document media can be found after relaunch. The chat header no longer labels normal native socket connection startup as `waiting for network`; it falls back to presence text unless the socket is actually closed/disconnected. Revalidated with targeted `xcrun swiftc -parse`, `git diff --check`, `python3 -m json.tool tests.json`, and a device-sdk `xcodebuild -quiet -project Vibe.xcodeproj -scheme Vibe -sdk iphoneos -configuration Debug -derivedDataPath /tmp/vibe-ios-chat-cache-device CODE_SIGNING_ALLOWED=NO build`, which succeeded with existing warnings only; simulator `xcodebuild` is currently blocked by local CoreSimulator/simdiskimaged runtime errors before app code compiles.
- Fixed the Android and iOS native profile/call routing follow-up. Header avatar taps now open the existing native profile page in the standalone chat shell instead of being ignored, profile/header audio and video call buttons now route through the native call engines, and new-chat Call actions start native voice signaling instead of deferring back to the old JS path. The native call engines now push `call-start`, `call-accepted`, `call-end`, and `webrtc-signal` through the Phoenix user channel with queued delivery while the channel joins, and incoming user-channel call events are handed back to the native call/CallKit stores. iOS Info.plist and Android manifests now declare the call-related mic/camera/audio/background permissions, and Android retries the call after mic/camera permission grant from both chat and home search actions. Revalidated with baseline and final Android `./gradlew :app:compileDebugKotlin`, baseline and final iOS simulator `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-call-profile-... CODE_SIGNING_ALLOWED=NO build`, `plutil -lint ios/App/Info.plist`, and source review of the native profile/call routes.
- Updated native Android and iOS chat context-menu selection behavior. Android long-press context controls now stay anchored around the held message instead of jumping to the top-row layout, and the menu includes a native `Select` action. Android and iOS message lists now enter selection mode from that action, show a selection circle beside selectable rows, move row layout to make room while preserving outgoing bubble alignment, toggle selection from row/circle taps, prune stale selected ids when rows change, and emit `messageSelectionChanged` with the active state, selected count, and selected message ids. Revalidated with targeted Swift parsing, `./gradlew :app:compileDebugKotlin`, `./gradlew :app:assembleDebug`, full iOS simulator `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-chat-context-selection CODE_SIGNING_ALLOWED=NO build`, `git diff --check`, and `python3 -m json.tool tests.json`; builds succeeded with existing warnings only.
- Followed up the remaining iOS home-to-chat transition/cache regression. The iOS chat presenter now uses an over-fullscreen custom push so UIKit keeps the home list alive underneath, and the transition no longer moves the real presenting home view during push/pop. Cached home rows now restore immediately on launch and schedule background remote reconciliation without showing a loading spinner or replacing the list, identical remote rows are skipped, native table reloads preserve scroll offset, and metadata-only cache fallback no longer carries raw preview/message payloads that can break JSON storage. Revalidated with baseline and final full iOS simulator builds via `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-ios-transition-cache-baseline CODE_SIGNING_ALLOWED=NO build` and `/tmp/vibe-ios-transition-cache-fix`, targeted `xcrun swiftc -parse`, `git diff --check`, `python3 -m json.tool tests.json`, and simulator install/launch on booted iPhone 17 (`simctl launch` returned pid 37445); builds succeeded with existing warnings only.
- Fixed the remaining iOS large-chat open delay where the full-screen chat presentation could wait on native engine/decrypt work. `ChatConversationController` now configures the first route with route-only header/status state before the view appears, defers engine-driven header/status/pin/profile refresh until after the controller has appeared and the background row read has returned, and disables the chat list's duplicate realtime channel ownership so the root presenter owns channel open/close. `ChatListView` now sends surface bind/unbind and channel open/close work through a background serial queue instead of calling `ChatEngine.queue.sync` from UIKit lifecycle methods.
- Revalidated the non-blocking open change with a baseline `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-ios-nonblocking-open-baseline CODE_SIGNING_ALLOWED=NO build`, targeted `xcrun swiftc -parse ios/Sources/App/AppRootView.swift ios/ChatModule/ChatMainView.swift ios/ChatModule/ChatListView.swift`, `git diff --check`, `python3 -m json.tool tests.json`, final `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-ios-nonblocking-open-fix CODE_SIGNING_ALLOWED=NO build`, and simulator install/launch on booted iPhone 17 (`simctl launch` returned pid 20464). Full builds succeeded with existing warnings only.
- Fixed the follow-up iOS home-to-chat freeze path shown by the device logs. `AppShellCoordinator` now treats chat presentation as a single-flight operation, ignores duplicate opens for the same route, drops stale async presentation requests, recovers/dismisses any visible `ChatConversationController` that escaped active tracking, and makes the header back action fall back to dismissing its own controller if coordinator state is stale. Seeded chat preview rows no longer mark a chat history as fully loaded, so the list does not replace rows from a partial seed. Home refresh now only shows `Waiting for Network` for real offline-style URL errors, not timeouts/race failures. The home cache write now flushes to `UserDefaults`, falls back to metadata-only payloads if raw messages are not JSON-safe, and logs store/restore counts. Home previews also reject ciphertext-shaped preview/content values and use the native engine to decrypt a one-message preview when possible.
- Revalidated the follow-up with a baseline `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-ios-route-freeze-baseline CODE_SIGNING_ALLOWED=NO build`, targeted `xcrun swiftc -parse`, `git diff --check`, and final `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-ios-route-freeze-fix CODE_SIGNING_ALLOWED=NO build`; both full builds succeeded with existing warnings only.
- Fixed the iOS root chat/home loading path that could make the app feel frozen while opening large encrypted chats. Home rows are now persisted after a successful fetch and restored before the remote refresh on the next launch, refresh failures keep the existing list visible instead of replacing it with the empty state, and the home header switches to `Waiting for Network` when refresh fails. Chat row taps no longer synchronously build/decrypt full transient message rows on the main thread; the first visible chats seed only a small recent slice in the background and the first two visible chats prefetch full native history after a successful home refresh. Chat/history requests now use an explicit 18s timeout instead of waiting for long QUIC stalls. The native home table also skips identical reloads to reduce row flicker.
- Revalidated with targeted `xcrun swiftc -parse`, `git diff --check`, `python3 -m json.tool tests.json`, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-ios-home-cache-chat-preload CODE_SIGNING_ALLOWED=NO build` from `ios/`; the full build succeeded with existing deprecation warnings only.

Remaining:
- Real-device verification is still needed for the new native call route: open a direct chat, tap the avatar, start audio/video from the header/profile actions, grant permissions on Android, and confirm incoming call signaling/CallKit or Android call store behavior across two signed-in devices.
- Real-device verification is still needed for the reported physical-device freeze: launch from the home screen, confirm the chat list appears from cache after a cold restart, turn off network and confirm rows stay visible with the waiting-network header, then open a large chat and confirm the back button stays responsive while history loads.

2026-04-25

Completed:
- Followed up the native chat status/RTL/typing polish. Android message status now uses a fixed-width slot so time does not jump, sent/read checks are drawn from the same paths as the legacy `Icons.tsx`, the pending clock is smaller with one static short hand and one rotating hand, and status swaps use a small alpha/scale transition. RTL text rows now use a Telegram/WhatsApp-style two-line bubble structure with Persian text above and timestamp/checks on a separate bottom line, while English rows keep the compact inline structure with more spacing between text, time, and checks. Android peer typing now actually inserts the native typing row, and both Android/iOS engines only mark typing state as sent after the websocket topic is available so an early blocked typing event cannot suppress the next push. Revalidated with baseline and post-change `./gradlew :app:compileDebugKotlin`, targeted `xcrun swiftc -parse`, `./gradlew :app:assembleDebug`, `git diff --check`, `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`, final screenshot `/tmp/vibe_android_status_final.png`, and clean `adb shell logcat -d -v time AndroidRuntime:E '*:S'`.
- Followed up the iOS/Android native chat send queue and status handling. iOS `ChatEngine` now preserves same-user runtime transport config, accepts direct websocket transport, starts Packet runtime when packet mesh lacks a proxy port, falls back to direct websocket if Packet startup fails, treats `sent` as stronger than a visible error, keeps queued drafts for automatic replay, and exposes manual retry without leaving bubbles permanently stuck. iOS New Chat now seeds the peer public key into the engine before opening the chat, and both iOS and Android rows now show an animated pending clock plus an inline retry button with a small nudge when a final send error is reached. Answer for the transport question: realtime chat should stay on Phoenix websocket; HTTP is still right for bootstrap/history/search/key lookup. Revalidated with `./gradlew :app:compileDebugKotlin`, `./gradlew :app:assembleDebug`, targeted `xcrun swiftc -parse`, sandboxed and elevated `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-ios-send-queue-fix CODE_SIGNING_ALLOWED=NO build` where the elevated build succeeded, `git diff --check`, `python3 -m json.tool tests.json`, `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`, `adb shell monkey -p com.mohammadshayani.vibe -c android.intent.category.LAUNCHER 1`, `adb shell uiautomator dump /sdcard/window.xml`, and a clean `adb shell logcat -d -v time AndroidRuntime:E '*:S'`.
- Fixed the Android native chat send path that left messages stuck in queued/sending state. `ChatEngine.configure` now preserves same-user runtime transport fields instead of erasing them, packet-mesh startup is attempted from the realtime engine when the proxy port is missing, and a failed packet bootstrap falls back to direct websocket transport instead of leaving the chat in `native-config-missing`. Queued messages remain stored for automatic replay, move to a visible error state with Resend if the connection still is not ready, and a retry can move back to sending/sent. Revalidated with `./gradlew :app:compileDebugKotlin`, `./gradlew :app:assembleDebug`, `git diff --check`, `tests.json` parsing, `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`, and on-device DM testing: the old `no_native_socket` queue replayed after direct fallback, `QueueFix25` was resent to `sent`, and a fresh `QueueLive26` message rendered in the list with logcat showing `sendMessage pushed` then `status=ok nextStatus=sent`; AndroidRuntime crash logs were clean.
- Fixed the native New Chat/find-user flow on Android and iOS. Android search now resolves a user first and only creates/opens the DM when Message/Add Contact/Call is pressed, using the legacy-compatible `myId`/`friendId` chat-create payload. Android and iOS user/search/chat-home parsers now accept `username`, `handle`, `displayName`, `friendName`, avatar, and public-key field variants and no longer show UUIDs or `Unknown` as the visible user label. The iOS SwiftUI search modal now shows a found-user action view with Chat, Call, and Add Contact; the UIKit native new-chat parser got the same display/public-key normalization. Revalidated with baseline and post-change `./gradlew :app:compileDebugKotlin`, `./gradlew :app:assembleDebug`, targeted `xcrun swiftc -parse`, `git diff --check`, `tests.json` parsing, `adb install -r`, on-device UI text checks for `mohammad`, `User is in Vibegram`, `Message`, `Call`, `Add Contact`, `Added`, chat header `mohammad`, a screenshot at `/tmp/vibe_new_chat_actions.png`, and a clean `adb shell logcat -d -v time AndroidRuntime:E '*:S'`.
- Followed up the Android input recording/swipe/hold regression. Recording now overlays the measured input row instead of setting the input container to `GONE`, so the composer should not jump upward when hold-to-record starts. The upward auto-lock path and visible lock pill were disabled to avoid accidental lock/jumping during a normal hold, while slide-left cancel remains. Reply swipe now arms with a lower horizontal threshold and cancels long-press continuously while swiping. Long-press context controls now pin at the top of the overlay without hiding or animating the live message cell. Revalidated source with `./gradlew :app:compileDebugKotlin`, `./gradlew :app:assembleDebug`, `git diff --check` for the touched chat files, and `tests.json` parsing. Device screenshot/install verification could not be completed because `adb devices` returned no connected devices after the rebuild.
- Refactored the Android story flow to match the newer iOS two-page structure instead of the old basic chooser. `android/app/src/main/java/com/mohammadshayani/vibe/ui/StoryActivity.kt` now has a camera-first main page backed by CameraX preview/capture plus gallery import, a separate edit/composer page with a framed media card, retake/save/AI actions, caption input, and a richer publish sheet for audience and duration. The same pass also aligned Android story publishing with the real native/server contract by sending `media_url`, `media_type`, optional `caption`, `visibility`, and `duration` after upload instead of the old incorrect field names. Revalidated with `./gradlew :app:compileDebugKotlin`, `./gradlew :app:assembleDebug`, `git diff --check`, and `tests.json` parsing.
- Followed up the Android chat input refactor correction. Kept attach, text input, and right action as three separate siblings, normalized their visual height to the same 44dp control size, removed the inner send button from the text field, kept the right action switching between mic/send based on typed text, filled the attach/right button backgrounds instead of leaving the attach button transparent, reduced the center EditText default minimum height/padding, and stopped the row layout-transition that was causing input shifts. Also tightened chat row gesture handling so reply swipe cancels long-press and the hold menu opens without animating the live cell layout. Revalidated with `./gradlew :app:compileDebugKotlin`, `./gradlew :app:assembleDebug`, `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`, on-device hierarchy checks confirming matching attach/input/action bounds, an on-device Saved Messages send of `CodexInputSizeOk11`, and a clean `adb shell logcat -d -v time AndroidRuntime:E '*:S'`. Screenshot capture remained blocked by the current adb approval quota, and uiautomator could not regain an idle dump after the synthetic voice long-press, though the app stayed foreground and crash logs stayed clean.
- Followed up the Android chat/home polish loop on device. Removed RecyclerView item/fade animations for new chat rows, kept Saved Messages appends on the immediate optimistic path, added native reply metadata parsing/rendering plus swipe/context reply into the input banner, removed the full-container home loading spinner by seeding and preserving cached rows during foreground refresh, made home search filter real row content with focused top chrome and a clear action, seeded chat headers from the active app palette, and fixed voice by adding the Android `RECORD_AUDIO` manifest permission plus immediate mic permission prompting before long-press recording. Revalidated with `./gradlew :app:compileDebugKotlin`, `./gradlew :app:assembleDebug`, `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`, adb screenshots for no-spinner home, focused search, Saved Messages header, bottom send `NoFadeCheck10`, reply banner, microphone permission prompt, and live recording UI, plus `git diff --check`, `tests.json` parsing, and a clean `adb shell logcat -d -v time AndroidRuntime:E '*:S'`.
- Followed up the Android Saved Messages/chat polish on device. The saved home row no longer shows the private-note subtitle, Saved Messages uses the filled saved bookmark avatar in home and chat, the chat header hides call actions and exposes search plus clear actions, cached saved rows render immediately before the remote refresh, native search focuses and filters the list while hiding the composer, keyboard insets keep the composer above the IME, and saved sends append optimistically at the bottom without the unsafe crossfade/diff path. Revalidated with `./gradlew :app:assembleDebug`, `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`, adb screenshots for the home row, Saved Messages header, search mode, search exit, and final `LoopCheck8` send, plus a clean `adb shell logcat -d -v time AndroidRuntime:E '*:S'` after the final run.
- Migrated the real Android native chat module out of the Expo package and into the standalone app package. Copied the chat/list/profile/theme transport stack into `android/app/src/main/java/com/mohammadshayani/vibe/chat`, added a native compatibility layer for the old Expo view/event APIs, copied the module resources, removed the Giphy SDK dependency path, and replaced the basic standalone `ConversationActivity` with `ChatMainView`. `ChatHomeActivity` now renders chats/contacts through the migrated native home list instead of the old basic RecyclerView adapter, keeps tab labels visible with neutral tab colors, shows legacy-matched new-chat/story icons, and opens a native New Chat dialog backed by `/api/user/...` lookup plus `POST /api/chat`. Saved Messages now uses the migrated chat composer/send path, including a bottom navigation inset fix and input layout fix so the send button is tappable above the system nav bar. Revalidated with `./gradlew :app:compileDebugKotlin`, `./gradlew :app:assembleDebug`, `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`, on-device uiautomator text checks for `Chats`, `Search`, `Saved Messages`, `Contacts`, `Calls`, `Settings`, `New Chat`, and the saved-message send result `CodexAndroidCheck9` with timestamp `12:35 PM`; `adb logcat -d -v time AndroidRuntime:E '*:S'` was empty after the run.
- Followed up the Android standalone home-list swipe delay. `ChatHomeActivity.kt` no longer wraps the native home list in a second `SwipeRefreshLayout`, and `ChatHomeListView.kt` now uses a horizontal-aware refresh wrapper so sideways row swipes are not held by pull-to-refresh before `ItemTouchHelper` translates the cell. Revalidated with baseline and post-change `./gradlew :app:compileDebugKotlin` runs from `android/`; both succeeded, with only existing Kotlin warnings.
- Followed up the iOS native chat reply/status/swipe performance issue. Root/home chat rows no longer seed or prefetch encrypted histories, so background root-list work does not decrypt every chat; the selected chat now builds transient initial rows from the home message payload without caching or marking history loaded. `ChatEngine.swift` preserves top-level `reply_to_id` and reply preview fields for history/live rows, `ChatListView.swift` can synthesize reply banners from the rendered message list on first paint, and the fallback reply text is no longer the misleading `Original message`. `ChatListViewCells.swift` makes the status check glyph slightly larger and scale-only on status changes, and `ChatListView+Interactions.swift` tracks swipe reply movement with the finger direction in real time. Revalidated with targeted Swift parse checks for chat/app-shell sources, `git diff --check`, `tests.json` parsing, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-reply-status-swipe-fix CODE_SIGNING_ALLOWED=NO build` from `ios/`. The first sandboxed build could not access CoreSimulator cleanly, but the approved escalated build succeeded.

Remaining:
- No source-level blocker remains for this follow-up. Device/runtime visual verification is still useful for swipe feel and thermal behavior.

2026-04-24

Completed:
- Followed up the iOS native chat send morph/home-list polish for large text and replies. `ios/ChatModule/ChatListView.swift` now defers the forced bottom scroll for tall pending text sends, projects the morph target above the composer while the overlay runs, then settles to bottom after reveal; it also captures reply sends before dismissing the input reply banner and synthesizes reply preview metadata from the current row set. `ios/ChatModule/ChatListViewModels.swift` parses reply ids/preview fields, and `ios/ChatModule/ChatListViewCells.swift` renders a native reply preview inside text bubbles and includes it in the send-morph content snapshot while keeping it out of the background snapshot. `ios/ChatModule/TelegramSendMorphProfile.swift` delays destination content fade-in proportionally for large height expansion, and status check icons now scale/fade in immediately when pending/sending changes to sent/delivered/read. `ios/ChatModule/ChatInputBar.swift` pins attach, GIF/send, and mic controls to the bottom of expanding multiline input. `ios/Sources/App/AppRootView.swift` and `ios/Sources/Home/ChatHomeCardCell.swift` add a visible tap flash/haptic and native long-press context menu for home chat cards. Revalidated with `git diff --check -- ios/ChatModule/ChatListView.swift ios/ChatModule/ChatListViewModels.swift ios/ChatModule/ChatListViewCells.swift ios/ChatModule/TelegramSendMorphProfile.swift ios/ChatModule/ChatInputBar.swift ios/Sources/App/AppRootView.swift ios/Sources/Home/ChatHomeCardCell.swift`, targeted `xcrun swiftc -parse` checks for the chat module and app shell sources, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-sendmorph-reply-large CODE_SIGNING_ALLOWED=NO build` from `ios/`. The Xcode build reached Swift compilation and still failed only in `actool` because CoreSimulator reports no available simulator runtimes for `iphonesimulator`.
- Tightened the iOS Telegram-style send/open follow-up. `ios/ChatModule/ChatListView.swift` now keeps the send transition overlay above the message list but below the native composer, so the outgoing bubble/ghost is visually tucked behind the input bar during the morph; it also anchors the first nonzero list layout to the bottom to avoid the visible mid-content-to-bottom jump. `ios/ChatModule/TelegramSendMorphProfile.swift` keeps the destination bubble plate opaque, burns off the source background/text faster, and shortens the final overlay reveal so the status/check glyph does not linger through a slow fade. `ios/Sources/App/AppRootView.swift` now applies route preview rows immediately, moves encrypted/native row reads and chat-channel opening off the main presentation path, and settles the initial bottom scroll during layout instead of a delayed post-appear dispatch. Revalidated with `git diff --check -- ios/ChatModule/TelegramSendMorphProfile.swift ios/ChatModule/ChatListView.swift ios/Sources/App/AppRootView.swift`, both targeted `xcrun swiftc -parse` checks for the chat module and app shell sources, and `node -e "JSON.parse(require('fs').readFileSync('tests.json','utf8')); console.log('tests.json ok')"`. `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-sendmorph-open-followup CODE_SIGNING_ALLOWED=NO build` reached Swift compilation and still failed only in asset catalog compilation because CoreSimulator has no available simulator runtimes.
- Followed up the iOS send-morph/list tail regression. `ios/ChatModule/ChatEngine.swift` no longer re-shapes history rows together with hidden live rows, so reopening a chat cannot let a hidden/newer live row steal the tail from the last rendered history item; native history rows are still shaped once in their visible history order so only the last same-sender item owns the tail. `ios/ChatModule/ChatListView.swift` keeps the pending native outgoing shape patch localized and lengthens the final overlay fade to `0.16s` so the status icon dissolves from the overlay into the real cell instead of snapping. Revalidated with `git diff --check -- ios/ChatModule/ChatListView.swift ios/ChatModule/ChatEngine.swift ios/ChatModule/TelegramSendMorphProfile.swift ios/ChatModule/ChatListViewCells.swift`, `xcrun swiftc -parse -target arm64-apple-ios26.0 -sdk /Applications/Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/Developer/SDKs/iPhoneOS26.2.sdk ios/ChatModule/ChatEngine.swift ios/ChatModule/ChatListView.swift ios/ChatModule/ChatListViewCells.swift ios/ChatModule/ChatListViewModels.swift ios/ChatModule/TelegramSendMorphProfile.swift ios/ChatModule/ChatInputBar.swift`, `node -e "JSON.parse(require('fs').readFileSync('tests.json','utf8')); console.log('tests.json ok')"`, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-sendmorph-tail-status-fix CODE_SIGNING_ALLOWED=NO build` from `ios/`. The Xcode build reached Swift compilation and still failed only in `actool` because CoreSimulator has no available simulator runtimes.
- Tightened the iOS native send-morph snapshot path for the reported bubble mismatch while preserving normal grouped bubble/tail styling. `ios/ChatModule/ChatListView.swift` now positions the temporary render cell at the settled target bubble rect before snapshotting, keeps the `8.0` top-right radius only for outgoing bubbles that continue a same-sender sequence, applies the same localized sequence shape to pending native outgoing rows and their adjacent previous outgoing row, applies status changes immediately, and shortens the final overlay fade. `ios/ChatModule/ChatEngine.swift` now shapes native/decrypted history and live rows as sender sequences instead of emitting every message with `showTail: true` and `borderTopRightRadius: 18`; grouped outgoing rows use top-right `8`, only sequence ends keep tails, history/live rows are refreshed together after inserts/deletes/history loads, and the history `compactMap` has an explicit `[[String: Any]]` result annotation to fix Swift's `Generic parameter 'ElementOfResult' could not be inferred` compile error. `ios/ChatModule/ChatListViewCells.swift` keeps timestamp/status metadata out of the destination background snapshot and leaves it only in the destination content snapshot. `ios/ChatModule/TelegramSendMorphProfile.swift` uses a shorter `0.36s` morph with faster destination background/content fade-in and shorter source fade-out. Revalidated with `git diff --check -- ios/ChatModule/ChatListView.swift ios/ChatModule/ChatEngine.swift ios/ChatModule/TelegramSendMorphProfile.swift ios/ChatModule/ChatListViewCells.swift`, `xcrun swiftc -parse -target arm64-apple-ios26.0 -sdk /Applications/Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/Developer/SDKs/iPhoneOS26.2.sdk ios/ChatModule/ChatEngine.swift ios/ChatModule/ChatListView.swift ios/ChatModule/ChatListViewCells.swift ios/ChatModule/ChatListViewModels.swift ios/ChatModule/TelegramSendMorphProfile.swift ios/ChatModule/ChatInputBar.swift`, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-sendmorph-inference-fix CODE_SIGNING_ALLOWED=NO build` from `ios/`. The Xcode build reached Swift compilation without the generic inference error and failed only in `actool` because CoreSimulator has no available simulator runtimes.
- Refactored the standalone Android shell settings/home follow-up. `android/app/src/main/java/com/mohammadshayani/vibe/ui/ChatHomeActivity.kt` now always exposes a real `Saved Messages` chat row in the Chats tab, routes that row through `ConversationActivity.savedMessagesIntent(...)`, replaces dialog-based settings rows with page pushes, and uses repo-owned XML vector drawables for the bottom tabs and settings rows instead of built-in Android mock icons. Added `android/app/src/main/java/com/mohammadshayani/vibe/ui/SettingsDetailActivities.kt` for real native QR, connection, and secret-key detail pages, registered `ConnectionDetailActivity` in the manifest, and reused the existing QR renderer.
- Revalidated the Android refactor with `./gradlew :app:assembleDebug`, installed `android/app/build/outputs/apk/debug/app-debug.apk` on the connected Samsung device with `adb install -r`, relaunched with `adb shell monkey -p com.mohammadshayani.vibe -c android.intent.category.LAUNCHER 1`, confirmed `ChatHomeActivity` resumed, and confirmed fresh `adb logcat -d -v time 'AndroidRuntime:E' '*:S'` was empty. UI hierarchy inspection was blocked because the physical device was on the Samsung lock/SystemUI screen during the dump.
- Followed up the standalone iOS root/chat shell after the broken layout report. `ios/Sources/App/AppRootView.swift` no longer wraps the app shell in the behind-left story transform, clipping, shadow, black background, or bottom overlay search button, so the top/bottom edge artifacts from that custom root motion are removed. Search is now a native SwiftUI `Tab` with `role: .search`; selecting it switches back to Chats and presents the existing search sheet, which keeps it in the same tab material instead of a separate pill/button. The Settings tab now uses a native `person.circle.fill` fallback when no avatar image is available instead of drawing an empty custom circle.
- Replaced the crashy inline `AppStoryCameraController` with `ios/Sources/App/AppNativeStoryCameraView.swift`, a standalone SwiftUI-wrapped native camera page adapted from the legacy native story module lifecycle rather than the Expo wrapper. The new camera owns AVFoundation/PHPicker in a separate view, presents with `fullScreenCover` from Chats, emits captured image/video temp URLs into a native preview, and tears down the capture session by strongly capturing the session/output in `deinit` to avoid the weak-reference-during-deallocation crash.
- Revalidated this follow-up with `xcrun swiftc -parse -target arm64-apple-ios26.0 -sdk /Applications/Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/Developer/SDKs/iPhoneOS26.2.sdk ios/Sources/App/AppRootView.swift ios/Sources/App/AppNativeStoryCameraView.swift`, `plutil -lint ios/App/Info.plist`, `node -e "JSON.parse(require('fs').readFileSync('tests.json','utf8')); console.log('tests.json ok')"`, `git diff --check -- ios/Sources/App/AppRootView.swift ios/Sources/App/AppNativeStoryCameraView.swift ios/App/Info.plist ios/Vibe.xcodeproj/project.pbxproj progress.txt tests.json`, and both simulator/device `xcodebuild` passes. The Xcode builds reach Swift compilation and still fail only in asset catalog compilation because CoreSimulator has no available runtimes.
- Updated the standalone iOS root/chat shell in `ios/Sources/App/AppRootView.swift`: removed the Chats loading skeletons, changed the bottom search action into a separate icon-only circular control using the same bar material, switched the top-right story action to a circle system icon while preserving the compose/pen action, and made the Settings tab fallback render a real person avatar glyph instead of an empty circle.
- Replaced the story placeholder sheet with a native behind-left story camera page. The story page now sits under the tab/root shell, and opening it pushes the home/root view to the right with the same broad motion shape as the legacy JS story underlay. Added `NSCameraUsageDescription`, `NSMicrophoneUsageDescription`, and `NSPhotoLibraryUsageDescription` to `ios/App/Info.plist` to stop the privacy crashes shown in the device logs.
- Revalidated this pass with `plutil -lint ios/App/Info.plist`, `xcrun swiftc -parse -target arm64-apple-ios26.0 -sdk /Applications/Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/Developer/SDKs/iPhoneOS26.2.sdk ios/Sources/App/AppRootView.swift`, `git diff --check -- ios/Sources/App/AppRootView.swift ios/App/Info.plist`, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-shell-story-after CODE_SIGNING_ALLOWED=NO build`. The Xcode build still fails only at `actool` because CoreSimulator has no available simulator runtimes, matching the baseline failure before this patch.

Remaining:
- Live-verify the Android story refactor on device: open the story action from home, confirm CameraX preview appears on the main page, capture/import both paths move into the edit page cleanly, and confirm publish reaches the backend with the corrected story payload keys.
- Live-verify the newest iOS send transition on device: confirm the outgoing ghost appears behind the composer like Telegram, the destination bubble never turns transparent during long-text sends, the status/check reveal is nearly instant, and first chat open is already bottom-anchored without a visible jump.
- Live-verify the iOS send transition on device: send multiple adjacent Saved Messages with the keyboard open and confirm the source-to-bubble crossfade keeps the same tint, the asymmetric corners do not average into a different bubble, and the final reveal no longer pops from the overlay snapshot to a differently colored real cell.
- Live-verify on a real iPhone: confirm the tab/search material matches the attached 4-tabs/search reference, the root no longer shows the previous bubble/edge artifacts, the story action opens the native camera without the `AppStoryCameraController` weak-reference crash, and gallery/photo/video capture flows reach the preview.
- Live-verify on a real iPhone or a machine with working simulator runtimes: open Chats, confirm no skeleton rows appear during loading, confirm the search circle sits alongside the tab bar material cleanly, open the story action, confirm the root pushes right while the native camera page comes from behind-left, and test camera/photo/microphone permission prompts.

2026-04-23

Completed:
- Restored the standalone iOS settings shell and home follow-up after the previous regression. `ios/Sources/App/SettingsView.swift` is back on `SettingsNativeMainViewRepresentable` with the old root QR/Edit/header/avatar/navigation behavior and the privacy page is back on the native section-card shell, while the Face ID fix stays in place through `NSFaceIDUsageDescription` in `ios/App/Info.plist` and the main-actor-safe biometric toggle. `ios/Sources/App/AppRootView.swift` now removes the home spinner in favor of a safe skeleton state, swaps the Chats tab list body to a UIKit-backed `ChatHomeCardCell` table so the older home-row payload treatment returns inside the current SwiftUI shell, and adds a new bottom search action outside the existing 4-tab container that switches to Chats, opens the search sheet, and focuses the keyboard immediately. Revalidated with `git diff --check -- ios/Sources/App/SettingsView.swift ios/Sources/App/AppRootView.swift ios/App/Info.plist ios/Sources/Home/ChatHomeService.swift ios/ChatModule/ChatEngine.swift`, `swiftc -parse -target arm64-apple-ios26.0 -sdk /Applications/Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/Developer/SDKs/iPhoneOS26.2.sdk Sources/App/SettingsView.swift Sources/App/AppRootView.swift` from `ios/`, `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-regression-baseline CODE_SIGNING_ALLOWED=NO build` from `ios/`, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphoneos -configuration Debug -derivedDataPath /tmp/vibe-device-verify CODE_SIGNING_ALLOWED=NO build` from `ios/`. Both Xcode builds still fail only in `actool` because this machine currently has no usable CoreSimulator runtimes, so a real app boot pass is still blocked in this workspace.
- Fixed the current standalone iOS privacy/settings and `Saved Messages` follow-up issues. `ios/Sources/App/SettingsView.swift` now uses native SwiftUI `List`/`Section` navigation instead of the custom UIKit settings host, the privacy page is now a native SwiftUI list as well, the Face ID toggle stays on the main actor, and `ios/App/Info.plist` now includes `NSFaceIDUsageDescription` so the privacy page no longer crashes when biometrics are requested. On the chat side, `ios/ChatModule/ChatEngine.swift` now skips pinned-message and group-agent fetches for `saved_messages`, `ios/Sources/Home/ChatHomeService.swift` injects a real Saved Messages row into the home list and seeds chat history previews into `ChatEngine`, `ios/Sources/Home/ChatHomeListModels.swift` now derives friend titles and message previews more defensibly, and `ios/Sources/App/AppRootView.swift` now uses a bookmark avatar for Saved Messages. I also hardened the server-side fallbacks in `server/lib/vibe_web/controllers/chat_controller.ex` and `server/lib/vibe_web/controllers/group_agent_controller.ex` so stray `saved_messages` pinned/agent requests no longer return the old forbidden path. Revalidated with `git diff --check -- ios/Sources/App/SettingsView.swift ios/Sources/App/AppRootView.swift ios/Sources/Home/ChatHomeListModels.swift ios/Sources/Home/ChatHomeService.swift ios/ChatModule/ChatEngine.swift ios/App/Info.plist server/lib/vibe_web/controllers/chat_controller.ex server/lib/vibe_web/controllers/group_agent_controller.ex`, `mix compile` from `server/`, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-privacy-savedmsg-fix CODE_SIGNING_ALLOWED=NO build` from `ios/`; that iOS build still fails only in `actool`/CoreSimulator because this machine currently has no working simulator runtimes, but it reaches Swift compile/link and does not report a Swift error for these changes.
- Replaced the iOS secret-key concealment effect with a direct port of `ios/Sources/App/gemini-code-1776957543775.html`. `ios/Sources/App/MetalKeyMaskView.swift` now renders the same secure-input style shell and particle field in Metal: 800 circle sprites, slow horizontal wrap, center-clustered vertical spread, slight sine wobble, additive blending, muted grey-to-white dots, and edge fade. I also moved the particle data into persistent `MTLBuffer`s instead of pushing the full array every frame. Revalidated with `git diff --check -- ios/Sources/App/MetalKeyMaskView.swift` and `xcodebuild -project ios/Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-gemini-final CODE_SIGNING_ALLOWED=NO build`; that build still stops in `actool` because this machine currently has no available CoreSimulator runtimes, but it reaches the Swift compile/link stages without reporting a Swift error for the new mask view.
- Reworked the Android welcome/auth surface around the user-requested motion and GPU treatment. `android/app/src/main/java/com/mohammadshayani/vibe/ui/WelcomeActivity.kt` now uses the new `WelcomeGpuBackdropView.kt` OpenGL ES backdrop instead of the old canvas gradient, keeps the copy near center with smaller type, and animates the three-word `Light in darkness.` line word-by-word over a darker top-right beam. `android/app/src/main/java/com/mohammadshayani/vibe/ui/AuthActivity.kt` now uses a translate-only bottom-sheet animation, forces the sheet to roughly `90vh` (`design_bottom_sheet` dumped at `[0,232][1080,2316]` on the connected Samsung), stores `loginSecret` the same way iOS does after sign-in/sign-up, and drops the blocking recovery dialog in favor of the iOS-style saved-secret flow. `android/app/src/main/java/com/mohammadshayani/vibe/ui/ChatHomeActivity.kt` now exposes a `Secret Key` settings row for the stored secret, and `android/app/src/main/java/com/mohammadshayani/vibe/storage/SecureKeyStore.kt` now clears `loginSecret` on sign-out alongside the other sensitive values. Revalidated with `git diff --check -- android/app/src/main/java/com/mohammadshayani/vibe/ui/WelcomeActivity.kt android/app/src/main/java/com/mohammadshayani/vibe/ui/WelcomeGpuBackdropView.kt android/app/src/main/java/com/mohammadshayani/vibe/ui/AuthActivity.kt android/app/src/main/java/com/mohammadshayani/vibe/ui/ChatHomeActivity.kt android/app/src/main/java/com/mohammadshayani/vibe/storage/SecureKeyStore.kt android/app/src/main/res/values/themes.xml android/app/src/main/res/anim/vibe_sheet_slide_up.xml android/app/src/main/res/anim/vibe_sheet_slide_down.xml`, `/Users/mohammadshayani/.gradle/wrapper/dists/gradle-8.14.3-bin/cv11ve7ro1n3o1j4so8xd9n66/gradle-8.14.3/bin/gradle -p android :app:assembleDebug --offline`, `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`, `adb shell monkey -p com.mohammadshayani.vibe -c android.intent.category.LAUNCHER 1`, and `adb shell uiautomator dump ...` on the connected Samsung. Both auth modes opened without an `AndroidRuntime` crash. I did not execute a real successful sign-up/sign-in against the live backend in this pass because that would require creating or using a real account secret on the production-facing API.
- Fixed the Android auth-sheet crash and aligned the welcome/auth copy with iOS. `android/app/src/main/java/com/mohammadshayani/vibe/ui/AuthActivity.kt` now attaches the `TextInputEditText` to `TextInputLayout` with `LinearLayout.LayoutParams` instead of a generic `ViewGroup.LayoutParams`, which stops the `ClassCastException` that was killing the app as soon as `Sign In` or `Create Account` opened the bottom sheet. The same pass updates the Android welcome/auth strings to match the current iOS wording more closely. Revalidated with `git diff --check -- android/app/src/main/java/com/mohammadshayani/vibe/ui/AuthActivity.kt android/app/src/main/java/com/mohammadshayani/vibe/ui/WelcomeActivity.kt`, `/Users/mohammadshayani/.gradle/wrapper/dists/gradle-8.14.3-bin/cv11ve7ro1n3o1j4so8xd9n66/gradle-8.14.3/bin/gradle -p android :app:assembleDebug --offline`, `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`, and unsandboxed `adb` verification on the connected Samsung device. The device repro that previously crashed on `Sign In` now opens the slide-up `Sign In` and `Create Account` sheets without a fatal exception.
- Reworked the Android standalone app into the same broad shell shape as iOS. `android/app/src/main/java/com/mohammadshayani/vibe/ui/ChatHomeActivity.kt` now hosts a native four-tab root (`Contacts`, `Calls`, `Chats`, `Settings`) with one shared content host, `ConversationActivity.kt` now opens a root-level read-only chat screen for chat rows and `Saved Messages`, `AppearanceSettingsActivity.kt` plus `AppTheme.kt` now mirror the iOS mode/plate model and sync the selected plate into the existing native chat theme prefs, and `MainActivity.kt`, `AuthActivity.kt`, and `WelcomeActivity.kt` now apply the stored appearance before routing into the shell. `android/app/src/main/java/com/mohammadshayani/vibe/home/ChatHomeListModels.kt` also now derives preview text from preview rows before falling back to empty copy. Revalidated with `git diff --check -- android/app/src/main/java/com/mohammadshayani/vibe/MainActivity.kt android/app/src/main/java/com/mohammadshayani/vibe/home/ChatHomeListModels.kt android/app/src/main/java/com/mohammadshayani/vibe/ui/AppTheme.kt android/app/src/main/java/com/mohammadshayani/vibe/ui/AppearanceSettingsActivity.kt android/app/src/main/java/com/mohammadshayani/vibe/ui/AuthActivity.kt android/app/src/main/java/com/mohammadshayani/vibe/ui/ChatHomeActivity.kt android/app/src/main/java/com/mohammadshayani/vibe/ui/ConversationActivity.kt android/app/src/main/java/com/mohammadshayani/vibe/ui/WelcomeActivity.kt android/app/src/main/AndroidManifest.xml` and `/Users/mohammadshayani/.gradle/wrapper/dists/gradle-8.14.3-bin/cv11ve7ro1n3o1j4so8xd9n66/gradle-8.14.3/bin/gradle -p android :app:assembleDebug --offline`, which succeeded.
- Reworked the iOS settings/chat follow-up after the first avatar workaround. `ios/Sources/App/SettingsView.swift` now keeps the root QR/Edit controls in the SwiftUI `NavigationStack` toolbar again, while `ios/Sources/App/SettingsNativeComponents.swift` isolates the avatar hero below the nav bar so it no longer intersects toolbar masking and adds stronger native row press feedback. The same pass updates `ios/Sources/App/AppRootView.swift` so the chats header reaches a real connected state, list previews derive from actual preview rows instead of falling back to `No messages yet`, empty states use the bundled cat Lottie treatment with tighter copy, and the Settings tab falls back to a person avatar glyph instead of an empty circle. `ios/ChatModule/ChatMainView.swift` also stops showing sticky `connecting...` copy once presence/history context is already available. Revalidated with `xcrun swiftc -parse ios/Sources/App/SettingsView.swift ios/Sources/App/SettingsNativeComponents.swift ios/Sources/App/AppRootView.swift ios/ChatModule/ChatMainView.swift`, `git diff --check -- ios/Sources/App/SettingsView.swift ios/Sources/App/SettingsNativeComponents.swift ios/Sources/App/AppRootView.swift ios/ChatModule/ChatMainView.swift`, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO` from `ios/`, which succeeded.
- Fixed the iOS settings avatar/header regression introduced when the root QR/Edit controls moved into SwiftUI toolbar items. `ios/Sources/App/SettingsNativeComponents.swift` now hosts the root QR and Edit glass buttons inside the native settings surface again, so the avatar hero scroll/morph stays isolated from the SwiftUI navigation bar layer and its masking/compositing behavior. `ios/Sources/App/SettingsView.swift` now routes those root actions back through the native settings host while keeping the NavigationStack-driven detail headers intact. Revalidated with `xcrun swiftc -parse ios/Sources/App/SettingsNativeComponents.swift ios/Sources/App/SettingsView.swift`, `git diff --check -- ios/Sources/App/SettingsNativeComponents.swift ios/Sources/App/SettingsView.swift`, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO` from `ios/`, which succeeded.
- Restored the iOS native wallpaper pipeline after the incorrect custom appearance experiment. `ios/Sources/App/AppRootView.swift` now passes `nativeThemeId` and `nativeThemeIsDark` to the chat surface instead of overriding gradients directly, `ios/Sources/App/AppRuntimeController.swift` no longer defines a separate wallpaper controller/model, and `ios/Sources/App/SettingsView.swift` is back to the supported appearance controls that map to the existing native theme plates. Revalidated with `xcrun swiftc -parse ios/Sources/App/AppRuntimeController.swift ios/Sources/App/AppRootView.swift ios/Sources/App/SettingsView.swift`, `git diff --check -- ios/Sources/App/AppRuntimeController.swift ios/Sources/App/AppRootView.swift ios/Sources/App/SettingsView.swift`, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO` from `ios/`, which succeeded.
- Added repo-scoped Codex native mobile guidance based on current OpenAI Codex docs plus local design references. The repo now has `.agents/skills/native-mobile-design/SKILL.md`, `references/platform-and-style.md`, and `agents/openai.yaml`, which steer Codex toward SwiftUI / Kotlin-native APIs, system components, restrained typography, and the calmer visual language seen in `/Users/mohammadshayani/Desktop/trade/trade-tauri/src/styles/palette.css`, `layout.css`, and `/Users/mohammadshayani/Desktop/trade/client/app/globals.css`. Root `AGENTS.md` also now includes a `Native Mobile UI` section so repo-level instructions reinforce the same guidance during native iOS/Android work. Revalidated with `git diff --check -- AGENTS.md .agents/skills/native-mobile-design/SKILL.md .agents/skills/native-mobile-design/references/platform-and-style.md .agents/skills/native-mobile-design/agents/openai.yaml`.
- Replaced the standalone iOS root chat overlay with an always-mounted UIKit presenter bridge so `Saved Messages` and normal chats can open from any tab through the real app root instead of a SwiftUI sibling layer that could fail to appear. `ios/Sources/App/AppRootView.swift` now mounts `RootChatPresenterBridge` as a root `ZStack` sibling above the `TabView`, logs bridge make/update/viewDidLoad/present/defer/flush events, and lets `RootChatPresenterController` fall back to the app key window when resolving the presenting controller. Revalidated with `xcrun swiftc -parse ios/Sources/App/AppRootView.swift`, `git diff --check -- ios/Sources/App/AppRootView.swift`, and `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO` from `ios/`, which succeeded and produced `~/Library/Developer/Xcode/DerivedData/Vibe-fnzhwyvbbieesmbtzbicdjofxhuo/Build/Products/Debug-iphonesimulator/Vibe.app`. An unsandboxed `xcrun simctl boot ...` request succeeded, but the follow-up unsandboxed install/launch requests were rejected, so app launch could not be verified in this session.
- Fixed the new iOS compile regression introduced by the expanded root-chat diagnostics. `ios/Sources/App/AppRootView.swift` now uses `NSCoder.string(for:)` for `CGRect` / `UIEdgeInsets` logging instead of the obsolete `NSStringFromCGRect` / `NSStringFromUIEdgeInsets` helpers, which restores compatibility with the current iOS SDK while keeping the added route/layout logs intact. Revalidated with `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO` from `ios/`, which now succeeds and produces `~/Library/Developer/Xcode/DerivedData/Vibe-fnzhwyvbbieesmbtzbicdjofxhuo/Build/Products/Debug-iphonesimulator/Vibe.app`. A follow-up simulator boot/install/launch check could not be completed in this session because unsandboxed `xcrun simctl` access was required and the escalation request was rejected.
- Expanded the standalone iOS root-chat diagnostics for the still-hidden `Saved Messages` route. `ios/Sources/App/AppRootView.swift` now logs root overlay appear/disappear, `UIViewControllerRepresentable` update state, native controller `viewWillAppear`/`viewDidAppear`/`viewDidLayoutSubviews`/`viewDidDisappear`/`didMove(toParent:)`, post-`applyRoute` surface configuration, row-source counts, chat-channel open snapshots, and live view/mainView geometry so the next repro will show whether the failure is in overlay presentation, UIKit appearance/layout, or chat-row hydration. Revalidated with `xcrun swiftc -parse ios/Sources/App/AppRootView.swift` and `git diff --check -- ios/Sources/App/AppRootView.swift` from repo root.
- Fixed the latest standalone iOS chat-shell regression where opening `Saved Messages` from `Settings` could still feel nested under the current tab or leave the settings surface visible behind an empty chat layer. `ios/Sources/App/AppRootView.swift` no longer wraps the entire tab shell in a root `NavigationStack`; it now mounts `ChatConversationScreen` in a top-level `ZStack` overlay with a higher z-order and disables tab-shell hit testing while a chat is open, so chats always render from the app root and can be opened from any tab without reusing the current tab stack. Revalidated with `xcrun swiftc -parse ios/Sources/App/AppRootView.swift` and `git diff --check -- ios/Sources/App/AppRootView.swift` from repo root. A fresh `xcodebuild -project Vibe.xcodeproj -scheme Vibe -destination 'generic/platform=iOS' ...` rerun is still blocked in this workspace by the local CoreSimulator/actool environment (`No available simulator runtimes for platform iphonesimulator`).
- Fixed the immediate compile follow-up from the new root chat route. `ios/Sources/App/AppRootView.swift` now makes `PresentedChatRoute` conform to `Hashable` as well as `Identifiable`, which satisfies `navigationDestination(item:)` for the root-level chat page binding. Revalidated with `xcrun swiftc -parse ios/Sources/App/AppRootView.swift` from repo root.
- Wired `ios/App/logo.PNG` into both native app icon pipelines. Added `ios/App/Assets.xcassets/AppIcon.appiconset` with the required iPhone/iPad/marketing icon sizes, registered the asset catalog in `ios/Vibe.xcodeproj/project.pbxproj`, generated Android launcher PNGs in `android/app/src/main/res/mipmap-*`, and updated `android/app/src/main/AndroidManifest.xml` to use `@mipmap/ic_launcher` and `@mipmap/ic_launcher_round`. Revalidated with `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/vibe-app-icon-ios` from `ios/` and `/Users/mohammadshayani/.gradle/wrapper/dists/gradle-8.14.3-bin/cv11ve7ro1n3o1j4so8xd9n66/gradle-8.14.3/bin/gradle -p android :app:assembleDebug --offline` from repo root.
- Fixed the remaining standalone iOS `Saved Messages` presentation regression from `Settings`. `ios/Sources/App/AppRootView.swift` now treats conversation pages as a real root-level destination above the tab shell: the app root owns a dedicated `PresentedChatRoute`, wraps the tab shell in a root `NavigationStack`, and pushes `ChatConversationScreen` through `navigationDestination(item:)` instead of trying to switch to the `Chats` tab or mount the chat as an overlay/modal layout artifact. `ChatConversationController` also now prefers the injected close callback so the root route binding can clear cleanly when the native header back action fires. Revalidated with `xcrun swiftc -parse ios/Sources/App/AppRootView.swift` from repo root. A full `xcodebuild -destination 'generic/platform=iOS' ...` rerun was blocked in this workspace by the local Xcode/CoreSimulator asset-catalog environment (`No available simulator runtimes for platform iphonesimulator`).
- Fixed the standalone iOS root chat route regressions around `Saved Messages`. `ios/Sources/App/AppRootView.swift` no longer wraps the tab shell in a root `NavigationStack(path:)`, which was crashing with `SwiftUI.AnyNavigationPath.Error.comparisonTypeMismatch` before the chat controller even mounted, and it no longer relies on `fullScreenCover`, which could build `ChatConversationController` without ever visibly replacing the settings page. The shell now keeps a single root-level `PresentedChatRoute` and mounts `ChatConversationScreen` directly in a top-level `ZStack` overlay above the tabs, while `AppShellCoordinator.openChat(...)` opens that overlay directly from any tab and `ChatConversationController` dismisses through an injected close callback when it is not inside a navigation controller. `ChatsRootView` and settings keep using `coordinator.openChat(...)`, so `Saved Messages` and normal chats now target the same real root chat surface without depending on a mixed SwiftUI path or presenter state. Revalidated with `xcodebuild -quiet -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/vibe-root-chat-overlay-fix` from `ios/`.
- Added targeted iOS route logging for the still-broken `Saved Messages` path. `ios/Sources/App/AppRootView.swift` now logs every app-shell chat open request, the request id used to trigger navigation, the chats-path replacement, the SwiftUI representable creation/update, and the native `ChatConversationController` lifecycle/apply/update/native-event/engine-change flow, including navigation-stack/root-controller context so it is obvious whether the chat controller is reaching the root-level shell. `ios/Sources/App/SettingsView.swift` now logs when the settings row opens `Saved Messages`, and `ios/ChatModule/ChatEngine.swift` now logs when chat channels open and whether `saved_messages` is correctly skipping realtime join. The same pass also changes the pending-route trigger from `chatId` to a monotonically increasing request id so reopening the same chat id cannot be silently ignored. Revalidated with `xcodebuild -quiet -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/vibe-saved-messages-route-logs` from `ios/`.
- Fixed the next standalone iOS follow-up around `Saved Messages` and the remaining safe-area bands. `ios/ChatModule/ChatEngine.swift` now treats `saved_messages` as an HTTP-only chat instead of a realtime Phoenix topic, so opening it no longer creates unnecessary socket demand or tries to join an invalid chat topic, while `ios/ChatModule/ChatListView.swift` tones down the top/bottom wallpaper safe-area overlays and removes the blur that was reading as extra header/footer edges. `ios/Sources/App/AppRootView.swift` now hides the tab bar background again, and `ios/Sources/App/SettingsView.swift` lets the native settings host extend through the container safe areas so the root page can own that layout itself. Revalidated with `xcodebuild -quiet -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/vibe-saved-messages-safe-area-fix` from `ios/`.
- Fixed the latest standalone iOS shell regression where opening a chat could immediately break navigation instead of presenting the real wallpaper/bubble chat view. `ios/Sources/App/AppRootView.swift` now keeps pushed conversations in normal `ChatMainView` chat mode instead of wrongly forcing standalone profile mode, ignores the profile/agent header actions that are not yet wired in the shell, and keeps `Saved Messages` routed into that same real chat surface. The same pass also removes the extra root settings navigation bar in `ios/Sources/App/SettingsView.swift`, restores visible bars only for pushed settings detail pages, tightens the bottom spacing in `ios/Sources/App/SettingsNativeComponents.swift`, and aligns the tab/root SwiftUI chrome backgrounds so the shell no longer adds the obvious top/bottom edge treatment on these pages. Revalidated with `xcodebuild -quiet -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/vibe-chat-layout-fix` from `ios/`.
- Restored the standalone iOS shell’s real native chat route. `ios/Sources/App/AppRootView.swift` keeps the tab shell but now pushes chats and `Saved Messages` into the extracted `ChatMainView` path, `ios/Sources/App/SettingsView.swift` continues routing `Saved Messages` through that same chat route, `ios/project.yml` / `ios/Vibe.xcodeproj/project.pbxproj` now include the reusable `ios/ChatModule/` sources in the app target, and the extracted chat surface no longer depends on Expo-only base classes. To get the target compiling, the pass also restored the native image/video edit payload modules, added a local `Lottie` compatibility shim plus sticker fallbacks for builds without the library, reused the existing `ChatAvatarURLResolver`, and switched the Phoenix proxy dictionary to the iOS-safe string keys already used by the Packet runtime. Revalidated with `xcodebuild -quiet -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/vibe-native-chat-route-7` from `ios/`.
- Replaced the standalone iOS settings root imitation with the old native host model. `ios/Sources/App/SettingsView.swift` now mounts `SettingsNativeMainViewRepresentable` instead of a SwiftUI `ScrollView` overlay, and `ios/Sources/App/SettingsNativeComponents.swift` now includes the old `NativeSettingsMainView`-style UIKit host where the avatar lives outside the scroll view, the hero spacer lives inside the scroll content, and `scrollViewDidScroll` drives the avatar morph. Revalidated with `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/vibe-settings-old-host` from `ios/`.

Remaining:
- Live-verify the new Android shell on device or emulator: confirm auth lands on the four-tab root, `Saved Messages` and normal chat rows open the new root conversation screen, appearance changes apply after returning from `Appearance`, and the settings rows feel correct under touch.
- If the new repo skill does not appear immediately in Codex skill pickers, restart Codex so it reloads `.agents/skills/native-mobile-design/`.
- Live-verify the restored root navigation chat route in the simulator or on device: tap `Saved Messages` from settings and a normal chat row from chats/contacts, confirm the conversation opens as a separate root page above the tab shell without switching the selected tab underneath, and confirm the native back action closes that page cleanly.
- Live-verify the fixed native chat push in the simulator or on device: tap a normal chat row and `Saved Messages`, confirm the shell now stays on the real wallpaper/bubble `ChatMainView` instead of bouncing/popping, confirm `Saved Messages` no longer hangs on a blank socket-backed screen, and confirm the chat header back action still returns to the chats list cleanly.
- Live-verify the restored settings host in the simulator or on device to confirm the avatar motion, the reduced top/bottom spacing, and the root/detail navigation bars now match the intended legacy structure under real scrolling. The build passes, but there has not been a manual interaction pass in this workspace yet.

2026-04-22

Completed:
- Fixed the current iOS settings follow-up issues. `ios/Sources/App/SettingsNativeComponents.swift` now hosts each native row inside a full-width container view with proper hugging/compression priorities so the chevron/toggle stay pinned to the trailing edge, and `ios/Sources/App/SettingsView.swift` now mirrors the old native settings structure more closely by keeping the morphing avatar as a top overlay with a scroll spacer underneath instead of embedding the hero inside the scroll content. The `Saved Messages` row no longer pushes the placeholder conversation route; it now returns to the main Chats page until the extracted live chat surface is linked into this target. Revalidated with `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/vibe-settings-fix-2` from `ios/`.
- Fixed the iOS settings page regression where the section cards rendered as empty space. `ios/Sources/App/SettingsNativeComponents.swift` now gives the native `UIViewRepresentable` row a real intrinsic height and `sizeThatFits` contract so SwiftUI no longer collapses the rows, and `ios/Sources/App/SettingsView.swift` continues to drive the page with the old settings sections while the avatar hero now reuses the legacy morphing avatar implementation and follows the scroll offset. Revalidated with `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/vibe-context-resume` from `ios/`.
- Replaced the placeholder standalone iOS shell with a parity-oriented native pass across auth, settings, and the tab pages. `ios/Sources/App/AuthViewController.swift` now stores the secret key silently and shows only a short in-app toast pointing users to `Settings > Secret Key`, `ios/Sources/App/SettingsView.swift` now uses the old settings row structure with profile editing, QR/key pages, appearance/media sections, and no root subtitles, and `ios/Sources/App/AppRootView.swift` / `AppRuntimeController.swift` now apply the old JS light/dark palette, shared inline page headers, chat/contact/call empty states, and reusable new-chat flow. Revalidated with `xcodebuild -quiet -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/vibe-ios-shell-parity-2` from `ios/`.
- Matched the iOS auth sheet header to the Resolo settings header more exactly. `ios/Sources/App/AuthViewController.swift` now uses a plain `NavigationStack` with the stock inline title bar, leading close control, and a unified background color instead of the custom toolbar background tint that was causing a visible top edge mismatch in the welcome/auth modal. Revalidated with `xcodebuild -quiet -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug build CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/vibe-auth-header-fix` from `ios/`.
- Followed up on the iOS welcome/auth visuals to match the Resolo native feel more closely. `ios/Sources/App/AuthViewController.swift` now uses the Resolo auth palette and control sizing with a large plain sheet instead of the earlier medium UIKit-style detent, and `ios/Sources/App/WelcomeViewController.swift` now uses a warmer multi-glow metal backdrop so the top-right light beam is visible in both light and dark appearances. Revalidated with `xcodebuild -project Vibe.xcodeproj -scheme Vibe -configuration Debug -sdk iphonesimulator -derivedDataPath /tmp/vibe-auth-welcome-followup CODE_SIGNING_ALLOWED=NO build` from `ios/`.
- Reworked the native welcome/auth entry flow on both platforms. `ios/Sources/App/AuthViewController.swift` now presents sign-in and sign-up as a SwiftUI sheet patterned after the Resolo modal with a standard navigation bar, clean floating inputs, and stock sheet chrome, while `ios/Sources/App/WelcomeViewController.swift` removes the footer blur, shortens the copy, darkens the background toward near-black, and tightens the metal light beam. `ios/Sources/Home/ChatHomeViewController.swift` now routes auth recovery through the same modal API. On Android, `android/app/src/main/java/com/mohammadshayani/vibe/ui/AuthActivity.kt`, `WelcomeActivity.kt`, and `ChatHomeActivity.kt` now share the same bottom-sheet auth presenter and welcome entry flow.
- Revalidated the refreshed native entry flow with `xcodebuild -project Vibe.xcodeproj -scheme Vibe -configuration Debug -sdk iphonesimulator -derivedDataPath /tmp/vibe-welcome-refresh-3 CODE_SIGNING_ALLOWED=NO build` from `ios/` and `/Users/mohammadshayani/.gradle/wrapper/dists/gradle-8.14.3-bin/cv11ve7ro1n3o1j4so8xd9n66/gradle-8.14.3/bin/gradle -p android :app:assembleDebug --offline`. Both builds succeeded. The iOS build emitted existing simulator-service noise and deprecation warnings, but no compile or link failures.
- Finished the native repo extraction for the old `vibe-chat-native` module. The remaining reusable Swift source and resources now live under `ios/ChatModule/`, the remaining reusable Kotlin source and Android resources now live under `android/chat-module/`, and the Expo bridge wrappers/package metadata now live under `legacy/mobile-rn/modules/vibe-chat-native/` instead of `mobile/modules/`.
- Renamed the extracted top-level native filenames to drop `Native` in the active native roots where practical, e.g. `ChatAgentView.swift`, `ChatHomeListView.swift`, `MusicPlayerStore.swift`, `VibeCallStore.swift`, `ChatHomeListView.kt`, `ChatInputBar.kt`, `CallEngine.kt`, and `VibeCallUiActivity.kt`.
- Updated `mobile/README.md` and `legacy/README.md` so the repo now documents `mobile/` as shared assets only, `ios/` and `android/` as the active native roots, and `legacy/mobile-rn/modules/vibe-chat-native/` as the archived Expo wrapper layer.
- Fixed the two final Android shell compile errors discovered during assemble verification: `ChatHomeService.fetchChats(...)` is now `internal` to match `ChatHomeListRow`, and `ChatHomeActivity` now uses Kotlin/Android `localizedMessage` instead of the Swift-only `localizedDescription`.
- Revalidated the native builds without npm. `xcodebuild -project Vibe.xcodeproj -scheme Vibe -configuration Debug -sdk iphonesimulator -derivedDataPath /tmp/vibe-native-ios-final CODE_SIGNING_ALLOWED=NO build` succeeded and produced `/tmp/vibe-native-ios-final/Build/Products/Debug-iphonesimulator/Vibe.app`. `/Users/mohammadshayani/.gradle/wrapper/dists/gradle-8.14.3-bin/cv11ve7ro1n3o1j4so8xd9n66/gradle-8.14.3/bin/gradle -p android :app:assembleDebug` succeeded outside the sandbox and produced `android/app/build/outputs/apk/debug/app-debug.apk`.

Remaining:
- The extracted `ios/ChatModule/` and `android/chat-module/` trees are now organized under the native roots, but they are not yet wired into the standalone app targets; the current native shell still compiles only the smaller top-level app/auth/home surface.
- A later pass is still needed if the hybrid Expo/native files left in `legacy/mobile-rn/modules/vibe-chat-native/ios` should be split further and pulled into the standalone targets instead of remaining archived as wrapper-coupled code.

2026-04-05

Completed:
- Added secure code-first connected-app guidance for standalone agents in `server/lib/vibe/ai/agent_builder.ex`. The builder prompt now steers custom website/app/backend requests toward user-controlled app code and env setup instead of hidden server-side tool mutation, and the integration payload now includes a concrete `connected_app_route_example` plus notes about validating `x-vibe-integration-secret` and exposing only explicit action ids.
- Added attachment-context routing for standalone-agent DMs in `server/lib/vibe_web/channels/chat_channel.ex` and `server/lib/vibe/ai/standalone_agent.ex`. Standalone agent dispatch now preserves `audio_urls` alongside image/document attachments, classifies common audio formats as `voice`, and appends a compact attachment summary to the current user turn so the model can reason about voice/audio/file/image inputs instead of silently losing them.
- Added voice-note agent routing in the normal main chat flow. `mobile/app/chat.tsx`, `mobile/app/chatlist.tsx`, and `mobile/modules/vibe-chat-native/ios/ChatEngine.swift` now send a short `agentText` summary like `User sent a 12-second voice message.` whenever a voice bubble is sent to a standalone agent DM, while keeping the exact existing native voice bubble UI and waveform behavior intact.
- Implemented the trade app’s secure connected-agent action surface at `/Users/mohammadshayani/Desktop/trade/client/app/api/vibe/agent-actions/route.ts`. The route validates `x-vibe-integration-secret`, supports the explicit safe actions `app.describe`, `website.summary`, `website.funnel.summary`, `website.live_presence`, `waitlist.summary`, and `engine.usage.summary`, and answers through Supabase service-role reads when available with the existing in-memory stores as fallback.
- Cleaned trade-app Vibe notification rendering in `/Users/mohammadshayani/Desktop/trade/utils/comprehensive_notifications.py`. The Vibe channel now strips HTML tags, converts `<br>`/block tags to plain newlines, unescapes entities, and sends plain text to Vibe instead of leaking raw markup like `<B>` into agent/event chat bubbles.
- Routed signal-only summaries to the same Vibe agent path in `/Users/mohammadshayani/Desktop/trade/utils/notification_integration.py`. Signal generation now emits a compact `signals` notification batch through the Vibe channel as well as the existing local/API broadcast path, so signal-only users hit the same external-agent delivery flow instead of Telegram-only behavior.
- Revalidated the external trade-app patches: `git diff --check` passed for the touched trade files, `./node_modules/.bin/eslint app/api/vibe/agent-actions/route.ts` passed in `/Users/mohammadshayani/Desktop/trade/client`, and `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile ...` passed for the patched Python files.

2026-03-26

Completed:
- Fixed a live standalone-agent DM crash in `server/lib/vibe/ai/standalone_agent.ex`. The DM delivery path was dereferencing `output.mediaUrl` directly on plain text outputs, which raised `KeyError` for normal text replies like the Trade agent introduction. Delivery now reads output fields defensively via helpers (`output_type`, `output_text`, `output_media_url`, `output_metadata`), so plain text, voice, and file outputs all persist/broadcast safely.
- Made standalone-agent DMs stateful going forward without weakening general chat encryption. `server/lib/vibe_web/channels/chat_channel.ex` now stores an `agentInputCiphertext` metadata field only for standalone-agent messages, encrypted with `Vibe.Chat.AgentMessageCrypto`. `server/lib/vibe/ai/standalone_agent.ex` now reconstructs recent DM context from prior agent plaintext plus decrypted `agentInputCiphertext` previews and passes that history into `ChatAgent.stream_response/3`, so the model can stop behaving like a brand-new invoke every turn.
- Tightened standalone-agent first-contact behavior. The standalone system prompt now explicitly tells the model not to reintroduce itself or restate capabilities in ongoing chats, and `welcome_message` is now treated as first-contact guidance only when the DM has no prior messages. This reduces the repeated “hi, how can I help?” behavior in existing agent DMs.
- Fixed a follow-up Swift compile regression in `mobile/modules/vibe-chat-native/ios/ChatNativeAgentStreamingText.swift`: `AgentCodeBlockView.isExpanded(...)` had a `storageKey` parameter that shadowed the static `storageKey(...)` helper, so Swift resolved the identifier as `String?` and produced `Cannot call value of non-function type 'String?'`. Qualifying the helper as `Self.storageKey(...)` fixed the collision. `xcrun swiftc -parse` and file-level diagnostics both passed after the patch.
- Upgraded the native iOS main-chat text bubble path in `mobile/modules/vibe-chat-native/ios/ChatListViewCells.swift` and `ChatListView.swift` so agent bubbles now render real fenced-code blocks with `AgentCodeBlockView`, ordinary text bubbles now use the shared markdown/link renderer instead of bold-only parsing, external text links now show a fixed-height native preview card populated through cached `LPMetadataProvider` metadata, and code-block expand/collapse now invalidates the collection layout while restoring the previous distance from bottom to avoid scroll jumps. `xcrun swiftc -parse`, file-level diagnostics, and `git diff --check` passed for the touched native chat files.
- Fixed the native main-chat polish issues in the shared iOS surface. `mobile/src/native/chat/mapper.ts` now normalizes timestamp labels back to `HH:mm` so agent rows stop rendering full ISO date-time strings, which also removes the oversized meta width that was compressing agent text bubbles. `ChatListViewCells.swift` now uses `ChatNativeAgentTextRenderer` for agent/mention bubble text so normal chat parsing matches the dedicated agent renderer, `ChatMainView.swift` now uses a steadier material blur with a softer header mask gradient, `ChatListView.swift` now forces bottom scroll for pending sends and applies resize inset changes before restoring scroll position, and `ChatNativeHomeListView.swift` now skips redundant inset rewrites that could churn the scroll offset. `xcrun swiftc -parse`, file-level diagnostics, and `git diff --check` all passed for the touched files.
- Fixed the saved-messages media bubble text regression in `mobile/app/saved-messages.tsx`. The saved-messages JS path had been sending placeholder bodies like `Image`, `Video`, `Voice Message`, and `Location`, and its native row mapper also fell back to `fileName` as bubble text. Saved media now matches the main chat path: image/video/voice/location rows send empty text unless there is a real caption, existing placeholder labels are suppressed at render time, and the non-native fallback renderer now uses the same normalized display text.
- Fixed blank/empty standalone-agent bubbles after reload in the main chat stack. `mobile/src/lib/ChatStore.ts` now normalizes API history messages before merging them into local state instead of rebuilding a stripped message shape that dropped `plaintext`, `plainContent`, `isAgentMessage`, `agentName`, and `agentId`. This keeps agent/event messages renderable after refresh instead of collapsing to timestamp-only bubbles.
- Fixed standalone-agent DM routing in the normal main chat and chat-list native surfaces. `mobile/app/chat.tsx`, `mobile/app/chatlist.tsx`, `NativeChatMainSurface.tsx`, `NativeChatSurface.tsx`, `ChatNativeMainModule.swift`, `ChatNativeListModule.swift`, `ChatMainView.swift`, `ChatListView.swift`, and `ChatEngine.swift` now carry `peerAgentId` through the native send path, cache it per chat, bypass the human-DM friend-key requirement for standalone-agent DMs, and automatically send `mentionedAgentId` / `agentText` so messages in a created agent DM route to that agent’s own prompt/runtime instead of only working inside the `/agent` builder chat.
- Hardened native agent-message detection in `ChatEngine.swift` for both live and history payloads by accepting snake_case flags plus `agentId` / `agentName` fields and `plaintext` fallbacks, so agent/event rows stay classified correctly even when payload shape varies.
- Added an incoming-chat toggle for standalone agents. Builder agent cards now include `incoming_chat_enabled`, `ChatListRow.AgentCard` parses/preserves it, and `ChatNativeAgentConfigPanel.swift` now exposes an `Incoming Chat` row that updates `approval_rules["chat_input"]["enabled"]` while preserving the existing inbox-batching config.
- Exposed standalone-agent messaging capability to the normal chat client. `server/lib/vibe/chat.ex` now includes `friendIsAgent`, `friendAgentId`, and `acceptsIncomingChat` in DM chat payloads for agent shadow users, and `server/lib/vibe_web/controllers/user_controller.ex` exposes `acceptsIncomingChat` on agent user payloads so the mobile client can reason about agent DMs directly.
- Enforced the incoming-chat toggle on the backend. `server/lib/vibe/agents.ex` now has `incoming_chat_enabled?/1`, `server/lib/vibe_web/channels/chat_channel.ex` rejects DM sends to standalone agents whose incoming chat is disabled before broadcast/persist, and `server/lib/vibe/ai/agent_builder.ex` includes the new toggle state in inline config cards.
- Disabled the normal native chat input bar when a DM peer is a standalone agent with incoming chat disabled. The main chat surfaces now switch the placeholder to `Messaging disabled for this agent` instead of presenting a working composer that the backend will reject.
- Fixed the external trade engine’s main Vibe notification path in `/Users/mohammadshayani/tradeai/trading_robot`. The runtime config now includes the `vibe` channel, the notifier defaults to `https://api.vibegram.io` instead of the old Railway host, normalizes a mistakenly configured full `/api/agents/...` URL back to the API base, and sends the same `User-Agent` / JSON headers as the working smoke test so Cloudflare treats engine notifications like the manual test path.
- Fixed the home-list agent naming mismatch in `server/lib/vibe/chat.ex`. `/api/chats/:user_id` now resolves DM friend payloads for agent shadow users through the owning `Agent` record, so `friendName` uses the agent `display_name` and `friendImage` prefers the agent avatar instead of the shadow username/invoke-style id.
- Fixed agent-originated push notification delivery on the server. `AgentEventRuntime` now follows the same notification path as normal chat messages for external-event agent posts, and `GroupAgent` now sends push notifications for agent replies instead of only broadcasting `user:new_message`.
- Fixed standalone agent naming at creation time: the builder now derives a practical fallback `display_name` from the user’s workflow description/persona instead of hard-defaulting to `New Agent`, and the builder prompt now explicitly tells the model to pass `display_name` whenever the user provided or corrected a name.
- Added native rename support to the standalone agent config panel in `ChatNativeAgentConfigPanel.swift`. The panel now shows a `Name` row, opens a native text-field alert, and saves the new name through the authenticated `PUT /api/agents/:id` API instead of forcing renames through chat only.
- Fixed cached-start home list refresh in `mobile/src/lib/ChatStore.ts`: when chats are already hydrated from AsyncStorage, `loadChats()` now schedules one short background remote `/chats` reconciliation instead of only returning cached rows forever, and any real remote reload clears that timer and updates the cooldown so realtime/manual refreshes do not double-fetch.
- Added unconditional JS-side chat refresh diagnostics in `mobile/src/lib/ChatStore.ts` so the app now logs `user:new_chat`, `user:new_message`, forced reload scheduling, and whether `loadChats` is using the cached path or bypassing it for a real remote fetch.
- Debugged the missing trade-event chat end to end: live `curl` to `/api/agents/:identifier/events` returned `200 messagePosted:true`, the live chat API confirmed the message is present in chat `73928163c120`, and the live `/api/chats/:user_id` response confirmed that chat is already in the owner-visible home list data.
- Added targeted native home-list diagnostics in `ChatNativeHomeModule`, `ChatNativeHomeListView`, and `ChatEngine` so the app now logs fetched chat ids, applied row ids, and whether engine change notifications hit an existing row or miss the current row set.
- Tightened the JS chat-list refresh path so `user:new_chat`, `user:new_message`, app-resume reloads, and pull-to-refresh can force a real remote `/chats` fetch instead of silently returning the cached non-empty list.
- Tightened standalone builder chat-id behavior: added a `validate_destination_chat` tool that re-reads the current owner-visible destination state, compares a provided chat id against live attached/default chats, repairs stale default destinations when needed, and explains when the same DM chat id is still correct.
- Fixed owner-facing agent destination chat exposure: attached/default chat ids shown by the builder now only come from chats shared by both the agent shadow user and the owning user, so the agent no longer hands out writable-but-invisible chat ids.
- Added an agentic destination-chat flow for standalone agents: the builder can now create or reuse the real owner<->agent DM, return the actual `chat_id`, and optionally set it as the agent's default destination chat instead of forcing a manual lookup step.
- Tightened DM resolution in `Vibe.Chat.find_chat_between_users/2` to only return `dm` rooms, then added `Vibe.Chat.ensure_dm_chat/2` so chat creation/reuse/restoration is consistent for builder and normal chat creation.
- Updated the normal chat controller to reuse `Chat.ensure_dm_chat/2`, preserving the restore/create/existing behavior while sharing the same deterministic DM logic.
- Added the new builder tool `ensure_destination_chat` and prompt guidance so when the owner asks for the chat id or easiest event destination, the agent can prepare the destination chat and return the real attached/default chat info directly in the inline config card.
- Fixed the native agent card tap path by disabling interaction on the decorative inner card container so the parent `UIControl` receives the press.
- Adjusted assistant row assembly so agent cards stay hidden until streamed text exists or streaming finishes, and action buttons render after the final assistant entry. The visible order is now `loading/streaming -> text -> card -> actions`.
- Deferred `agent_cards` channel events until the first streamed text chunk or finalization so the client no longer receives card events ahead of post-tool text.
- Fixed a Railway builder crash in `sanitize_builder_payload/1` by handling `DateTime`, `NaiveDateTime`, `Date`, `Time`, `Decimal`, and other structs safely instead of treating structs as enumerable maps.
- Added native agent stream failure handling so socket closes, channel errors, persisted stale streams, and the 120s timeout all resolve into a non-loading assistant state with retryable fallback text instead of hanging forever.
- Wired the expanded Vibe tab bar button into native agent streaming state so it switches to a stop action while a response is running and locally stops the current stream.
- Restyled the native agent config/env panel to match the builder setup panel palette and row treatment, including the same background, cards, divider color, prompt surface, and interaction highlight.
- Filled the env panel secret gap by letting native agent cards reuse the builder session `latestSecret` for the active agent and by accepting `secret`/`invoke_secret` fallback keys when cards are parsed.
- Added an authenticated `GET /api/agents/:id/secret` endpoint so the native agent config panel can fetch the current decrypted invoke secret for the signed-in owner instead of falling back to the hint suffix.
- Reworked the native agent config sheet into a more compact agent panel with a glass/metal-style secret surface, a masked `vas_...` placeholder, and explicit reveal/copy/rotate actions that use the real fetched secret.
- Cached builder secrets per agent id inside the native agent screen so a freshly created agent keeps its real secret available in the config panel even after builder state changes.
- Removed the automatic secret fetch when the config panel opens, removed the visible "tap to load" prompt, and changed the hidden secret cover to a detached glass canvas so its edges do not bleed into the touch layer.
- Updated native `builder_state` handling so `activeAgentId` and `latestSecret` from streamed builder events also populate the per-agent secret cache; this fixes copy/reveal for newly created agents before any rotate call.
- Added detached top and bottom gradient masks to the config panel, matching the attachment sheet pattern so the sheet header/body transition no longer shows hard edges.
- Cleaned external agent event text at ingestion time in `server/lib/vibe/ai/agent_event_runtime.ex`. Event `title`, `text`, and attachment captions now normalize rich-text/HTML-style inputs like `<B>`, `<br>`, and common entities into plain chat-safe text before persistence and posting, which prevents raw markup from leaking into Vibe chat bubbles.
- Added inbox-mode support to standalone agents without a separate management screen. `server/lib/vibe/ai/agent.ex`, `server/lib/vibe/ai/standalone_agent.ex`, and `server/lib/vibe_web/channels/chat_channel.ex` now support two owner-only live tools: `query_event_inbox` for answering historical notification questions from stored event threads, and `configure_event_inbox` for switching between `per_event` delivery and `batched_summary` delivery on a `4h` or daily cadence from the main chat.
- Added batched event-summary posting in `server/lib/vibe/ai/agent_event_runtime.ex` using `agent.approval_rules["event_inbox"]` / integration routing rules instead of a new schema. In batched mode, summarize-only events stop posting one bubble per event, accumulate inside the event thread state, and emit one summary bubble per cadence window with thread/source/window metadata.
- Propagated related-message metadata through standalone agent replies in `server/lib/vibe/ai/standalone_agent.ex` so inbox-query answers can attach `relatedMessageIds`, a title, and a subtitle to the final text response when matching underlying chat messages exist.
- Reused the native main chat’s existing inline accessory path for related-message navigation instead of creating a separate chat surface. `ChatListViewModels.swift` now parses related-message metadata, `ChatListViewCells.swift` renders a related-messages inline card, `ChatListView.swift` emits a `relatedMessagesPressed` native event, and `mobile/app/_components/ChatListScreen.tsx` shows the existing global banner for those related ids and jumps directly to the underlying bubbles in the current chat.
- Added a direct native inbox-mode control to `ChatNativeAgentConfigPanel.swift`. The config sheet now shows an `Inbox Mode` row with `Event bubbles`, `Batch summary every 4h`, and `Daily batch summary`, and saves the selection through `PUT /api/agents/:id` by updating `approval_rules["event_inbox"]`. Agent cards now carry `event_inbox_mode` and `summary_window_hours` so the sheet reflects the current backend mode immediately.
- Added a detailed web-side standalone agent config reference under `client/src/pages/AgentConfigDocs.tsx` and routed it at `/docs/agents/config`. The docs now explicitly cover identity fields, prompt/tool/output settings, delivery targets, inbox batching config, integration endpoints, secrets, and owner operations, and the main agent docs page now links into that reference.
- Fixed the web delivery path for `/` by updating the root `Dockerfile` to build the React client in a dedicated Node stage and copy the resulting `client/dist` bundle into `server/priv/static/` before the Phoenix release is compiled. I also ran `npm run build -- --outDir ../server/priv/static` from `client/`, which generated a fresh SPA bundle so the local Phoenix server can serve the landing/docs site instead of a stale or missing frontend bundle.
- Redesigned the standalone agent docs into a cleaner multi-document suite with a sticky minimal sidebar, a dedicated scrollable content pane, and smaller focused pages for overview, config reference, examples, and env/setup. Added `client/src/components/docs/DocsShell.tsx`, split routing across `/docs/agents`, `/docs/agents/config`, `/docs/agents/examples`, and `/docs/agents/env`, simplified the docs header so the sidebar owns docs navigation, and rewrote `client/src/pages/AgentDocs.css` around the new shell instead of the old hero/card marketing layout.
- Added explicit env/getting-started docs under `client/src/pages/AgentEnvDocs.tsx` with a copyable `.env` block, Python settings example, variable reference, and destination-chat guidance, so the docs now show both code examples and how to get/apply the env pack from the owner UI.
- Rebuilt the React client successfully with `npm run build` and regenerated the Phoenix-served static bundle with `npm run build -- --outDir ../server/priv/static`. The current workspace still contains unrelated local frontend edits and generated assets outside this docs change, so those were left out of the docs-only git staging plan.
- Fixed a live `/api/agents/:identifier/events` regression in `server/lib/vibe/ai/agent_event_runtime.ex`. The HTML entity decoder was piping text into `Regex.replace/4` with the arguments reversed, which caused normal event titles like `OPEN EURUSD` to raise a `FunctionClauseError` inside `decode_html_entities/1` and return `500`. Rewrote the numeric-entity replacement steps to call `Regex.replace/4` with the correct `regex, string, replacement` order and recompiled successfully with `mix compile`.
- Added a generic standalone-agent connected-app bridge in `server/lib/vibe/ai/tools/connected_app.ex` and wired it into `server/lib/vibe/ai/agent.ex`, `server/lib/vibe/ai/tool_registry.ex`, `server/lib/vibe/ai/standalone_agent.ex`, and `server/lib/vibe/agents.ex`. Standalone agents can now call a configured app-owned HTTPS endpoint through a new `call_connected_app` tool, using the integration’s existing encrypted secret for auth and reading endpoint/action config from `agent_integrations.routing_rules.connected_app` instead of direct Supabase access or trade-specific hardcoding. The standalone system prompt now injects the allowed connected-app actions per agent so the model uses only configured actions.
- Tested the published live agent `bd35d022-ad48-461a-ac44-f09d165a4232` on `https://api.vibegram.io` on 2026-04-08 with the provided invoke secret via `/api/agents/:identifier/invoke`. The HTTP calls succeeded, but the live replies explicitly said the agent has no connected-app or store-management tools and only exposes search/image/document/spreadsheet functions, so the deployed agent is still running the old trade/general capability set rather than the new shop-connected configuration.
- Added direct custom prompt editing to the native standalone-agent config sheet in `mobile/modules/vibe-chat-native/ios/ChatNativeAgentConfigPanel.swift`. The `Prompt` row now always appears, shows a condensed preview, opens an editable full-screen prompt editor, and saves `system_prompt` through the existing `PUT /api/agents/:id` owner API instead of keeping prompt control read-only.
- Reduced standalone-agent latency for simple config requests. `server/lib/vibe/ai/agent.ex` now exposes direct always-available `get_current_agent_config` and `update_current_agent_config` tools for the current agent’s prompt/name/status/config, so one-agent reads and edits no longer need to bounce through `builder_assistant`. I also relaxed the base tool guidance so delegation is reserved for multi-step builder/integration work, and `server/lib/vibe/ai/subagent_registry.ex` now passes `requester_user_id` plus the active agent id into delegated builder calls instead of forcing the builder to rediscover context.
- Reduced subagent overhead in `server/lib/vibe/ai/agent_builder.ex`. Internal `delegate_task/3` calls now use an ephemeral builder execution path with empty history instead of loading and persisting the full builder session for each subagent turn, and `subagent_mode` now uses a smaller `max_tokens`/`max_depth` budget (`900` / `3`) for faster specialized execution.

Remaining:
- The live published agent still needs owner-side reconfiguration before it can do shop work. Specifically, the deployed agent needs the correct shop system prompt, `call_connected_app` in `enabled_tools`, and a live connected-app integration whose `routing_rules.connected_app.endpoint_url` points at the deployed app endpoint. The current live secret alone is enough to invoke the agent, but not enough to mutate the live agent config without owner auth.
- Live-verify the low-latency path on a deployed server: ask a standalone agent in normal chat to change its prompt or rename itself, confirm it uses `update_current_agent_config` instead of `delegate_to_subagent`, and measure that the response returns quickly without the previous multi-minute builder handoff.
- Live-verify the new standalone-agent voice-note path on device: send a voice bubble to a standalone agent DM in the normal main chat (not `/agent`) and confirm the response uses the new attachment context instead of ignoring the voice note.
- Live-verify the trade app’s connected-agent route end to end by configuring `agent_integrations.routing_rules.connected_app.endpoint_url` to `/api/vibe/agent-actions`, then asking the agent for website summary, funnel summary, live presence, waitlist summary, and engine usage summary from a real chat.
- Decide whether the trade app also needs an explicit UI/docs surface for the new connected-agent code path. The secure backend route exists now, but there is not yet a dedicated trade-app settings page explaining the action ids and required env vars.
- Live-verify the exact server crash from the new log: send a normal standalone-agent DM that previously triggered `KeyError key :mediaUrl not found` in `StandaloneAgent.deliver_output_to_chat/4`, and confirm the agent now replies normally without the task crash.
- Live-verify standalone-agent DM continuity after the new encrypted preview metadata starts accumulating: ask the same agent a follow-up question in the same DM and confirm it no longer repeats the full onboarding/capability intro on every turn.
- Decide the next architecture step for agent-owned channels/groups. The current codebase already has channel primitives in `Vibe.Chat` and agent channel tools in `server/lib/vibe/ai/tools/channel.ex` (`post_to_channel`, `get_channel_analytics`, `schedule_channel_post`), but there is not yet a first-class flow where a standalone agent creates/attaches/manages a dedicated channel plus a paired chat assistant. That needs a separate product/backend pass.
- Implement the narrow app-side action endpoint in `/Users/mohammadshayani/Desktop/trade` for the new connected-app bridge. The Vibe side can now securely call configured app actions, but the trade app still needs to expose the actual action surface for things like website monthly summary, conversion funnel summary, waitlist summary, or other admin-safe metrics instead of requiring direct Supabase/database access from Vibe.
- Live-verify the new native rich-text bubble path on device or simulator: confirm agent messages with fenced code now render as distinct code cards in the main chat, ordinary user messages detect bare links and markdown links, preview cards open the in-app browser, and expanding/collapsing a code block keeps the scroll position stable instead of flickering or snapping.
- Live-verify the native main-chat polish pass on device or simulator: confirm agent bubbles now show time-only metadata, long agent text no longer looks horizontally compressed, header masking feels smoother with the adjusted blur/mask, normal sends force the list back to bottom even when scrolled up, and the chat/home lists no longer show the previous scroll flicker under real touch input.
- Live-verify the specific chat regression from the screenshot on device: open the standalone-agent DM that previously showed timestamp-only rows, force a reload/cold start, and confirm the historical trade/event messages now keep their plaintext instead of rendering empty bodies.
- Live-verify main-chat standalone-agent DM sending on device: send a normal text message in the created agent’s DM (not the `/agent` builder view), confirm the message no longer gets stuck behind the human-DM friend-key branch, and confirm the response comes from that standalone agent’s own prompt/runtime.
- Live-verify the new incoming-chat toggle end to end: disable `Incoming Chat` in the native config panel, confirm the main chat composer disappears or the server rejects sends cleanly, then re-enable it and confirm normal DM messaging resumes.
- Live-check the refreshed web landing/docs bundle in a browser after deploy. The source routes and built bundle now target `/` -> landing and `/app` -> chat/auth, but I did not run a real browser session in this workspace.
- Hard-refresh the deployed landing/docs site once the new bundle is live because the service worker may keep serving the previous docs shell until the cache updates.
- Re-run the trade/test `/events` curl against the deployed server and confirm the previous `Regex.replace/4` crash is gone and event titles/text containing either plain text or numeric HTML entities now ingest cleanly.
- Live-run the external trade engine again from `/Users/mohammadshayani/tradeai/trading_robot` and confirm the normal runtime now posts to Vibe as well as Telegram. The remaining config caveat is that `SETTINGS['send_notifications']` is still `False` in the default `development` environment, so the engine must be started with a notifying environment such as `TRADING_ENV=testing` or `TRADING_ENV=production` if the current launch path depends on `NotificationManager`.
- Live-verify the home list after one more remote `/api/chats/:user_id` refresh or external event send: the DM row that previously showed the invoke/shadow username should now show the agent `display_name` without needing a separate client-side remap.
- Live-send one agent event and one group-agent reply after deploy to confirm Expo push notifications now fire for agent-originated messages, not just `user:new_message` channel updates.
- If you want event summaries to be truly batched, that still needs a separate runtime change. The current `AgentEventRuntime` groups events by `thread_key` and keeps one evolving thread summary/state, but it still posts one summary/update per ingested event rather than waiting and emitting a delayed batch summary across multiple incoming events.
- Capture a fresh device log after another cold app start or external event send and verify the new cached-start reconciliation path: `loadChats using offline cached data ...`, then `scheduling background remote chat reconciliation`, then `background remote chat reconciliation firing`, then `loadChats begin ... forceRemote=true`, then `native fetchChats succeeded` with the missing agent chat id included. That will confirm the home list no longer needs a hot reload/manual refresh to pick up new remote chats.
- Live-validate the repeated chat-id flow in the app: ask the standalone agent whether a previously returned chat id is still correct, confirm it re-checks live state instead of repeating memory, and confirm it explains when the same DM id is expected versus when it repaired a stale default.
- Re-test the live flow with a freshly returned chat id from the agent after the owner-visible chat filter fix. The old chat id the user copied may still point at a chat the owner cannot access.
- Live-validate the new `ensure_destination_chat` builder flow in the app: ask the standalone agent for the chat id, confirm it creates or reuses the DM, updates the inline config card, and copies the returned default/attached chat id from the native config panel.
- Decide whether `get_integration_details` should stay read-only or automatically invoke `ensure_destination_chat` when the user clearly asks for the easiest setup path.
- Validate the iOS flow on device or simulator, especially card taps opening the config panel, cached secret reuse after agent creation, copy before rotate, secret reveal/copy/rotate, the detached glass overlay appearance, the masked header edge treatment, builder/subagent turns that emit cards before text, and the stop-streaming state in the Vibe tab bar.
- Live-validate the new event inbox flow in the main chat: send one external event containing HTML-like markup, confirm it renders as clean plain text, ask the standalone agent a historical inbox question like “yesterday how many trades were opened?”, then tap the related-messages inline card/banner to confirm the native list jumps through the underlying chat bubbles.
- Decide whether batched-summary mode also needs a dedicated stored-event inbox browser for summaries whose underlying events were intentionally not posted as individual chat bubbles. The current implementation supports silent storage plus batched summary posting and live query answers, but related-message jump navigation only works when matching chat message ids exist.
- `mix test` cannot run in `server/` because `server/config/test.exs` is missing.
- The full mobile TypeScript check still fails with existing repo-wide errors outside the touched native agent files; `git diff --check` passed for the changed files, but no simulator/device build was run.
- Continued the iOS root/story shell follow-up. `ios/Sources/App/AppRootView.swift` now keeps story inside layout with a leading-edge transition, hides the tab bar during story/edit mode, uses secondary/off text for the home Edit action, wires edit selection into the existing native home cells, sends Read/Mute/Delete bulk actions through the authenticated backend endpoints, and adds a Settings-style native header mask over the home list. `ios/Sources/App/AppVectorIcons.swift` now draws the story icon with the legacy plus-circle stroke shape instead of the previous generic filled circle.
- Adapted the legacy native story composer into `ios/Sources/App/AppNativeStoryComposerView.swift` as a standalone UIKit view with a SwiftUI wrapper. Edited image stories now render the composed native editor card into a temporary media file before publish, and `ios/Sources/App/AppNativeStoryCameraView.swift` uploads that media to `/api/media/upload` before posting the story payload to `/api/stories`. `ios/App/Info.plist` now also includes `NSPhotoLibraryAddUsageDescription` for the composer download action.
- Revalidated the touched iOS files with `xcrun swiftc -parse`, `plutil -lint ios/App/Info.plist`, and `git diff --check`. `xcodebuild -project Vibe.xcodeproj -scheme Vibe -sdk iphonesimulator -configuration Debug -derivedDataPath /tmp/vibe-story-native-followup CODE_SIGNING_ALLOWED=NO build` reached Swift compilation and failed only in asset catalog compilation because CoreSimulator has no available simulator runtimes on this machine.
- Continued native chat status/RTL polish. Android status glyphs now use a smaller fixed slot and smaller legacy check paths, Android RTL plain text bubbles use tighter padding and a shorter bottom meta gap, iOS now uses the same RTL bottom-meta cell structure, the iOS pending clock has one static hand plus one rotating hand, and both Android/iOS visible rows cascade older outgoing `sent`/`delivered` statuses to `read` when a later outgoing row is read. Android compile/assemble and a full iOS simulator build passed; Android device install is pending because no adb device is currently attached.
