Shared mobile assets live here.

All active Swift code now lives under `ios/`, and all active Kotlin code now lives under `android/`.
The archived Expo/React Native shell and Expo bridge wrappers live under `legacy/mobile-rn/`.
