{
  "name": "vibe-sandbox-browser",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright CDP driver invoked by sandbox-gateway inside the vibe-sandbox image",
  "main": "browser.js",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "playwright-core": "1.61.0"
  }
}
