# Must match the password embedded in core.env's and agent-runtime.env's
# VALKEY_URL (redis://:<password>@valkey:6379) — deploy/valkey/entrypoint.sh
# passes this to valkey-server as --requirepass.

VALKEY_PASSWORD=changeme
